# Example environment variables for StanceSense Core API
# Copy this file to .env and fill values for local development

# Google service account JSON file path (or rely on ADC)
GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account.json

# Application ID used for Firestore paths
__app_id=stancesense

# Optional: JSON Firebase config if you use it
#__firebase_config={}

# Gemini/Generative AI API key (if using RAG external calls)
GEMINI_API_KEY=

# Optional: tune RAG rate limit per minute
# RAG_RATE_LIMIT_PER_MIN=30
